<p style="color: green"><%= notice %></p>

<div id="daftars">
  <% @daftars.each do |daftar| %>
    <%= render daftar %>
    <p>
      <%= link_to "Show this daftar", daftar %>
    </p>
  <% end %>
</div>

<%= link_to "Daftar Sekarang", new_daftar_path, class: "btn btn-primary" %>
